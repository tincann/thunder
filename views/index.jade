extends layout

block content
  #wrapper
    #header
        #logo
        #facebook
            p Facebook
        #title
            p Login
    #content

        #loginform
          form(action='/login',method='post')

            #facebookid
            label(for='fbid') Facebook ID
                input(id='fbid',type='text',value='',placeholder='id',name='fbid')

            #facebooktoken
            label(for='fbid') Facebook Token
                input(id='fbid',type='text',value='',placeholder='token',name='fbid')

            #submit
            input(type='submit',value='Login',class='button')

        #recentlogin
            p recent
            #loginlist
                p empty

    #footer